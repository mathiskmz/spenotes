<% content_for :title, "Nouveau patient" %>

<%# Header %>
<div class="flex items-center justify-between px-4 py-4">
  <%= link_to patients_path, class: "text-sp-accent text-sm font-medium" do %>
    Annuler
  <% end %>
  <h1 class="text-base font-semibold text-sp-primary">Nouveau patient</h1>
  <div class="w-16"></div>
</div>

<div class="px-4">
  <%= render 'form', patient: @patient %>
</div>
